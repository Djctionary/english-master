# v0.2 — Learn Page Cleanup + TTS & Model Upgrade

**Date:** 29/03/2026

## Requirements

### Learn Page — Analysis Cleanup
1. Remove "Simply:" simplified sentence from analysis output
2. Remove word extraction reasoning (e.g. "This word may be unfamiliar to learners...")
3. Remove entire "Grammar & Phrases" section
4. Remove individual word/component explanations (e.g. "not always right·complement—What is said about the subject...")
5. Keep color-coded sentence displayed but **non-interactive** (no hover, no click)

### Infrastructure
6. Replace OpenAI TTS → **ElevenLabs Turbo v2.5** (fast + natural)
7. Upgrade analysis model: GPT-4o-mini → **GPT-4.1-mini**

## Implementation Plan

1. **`lib/openai.ts`** — Change model to `gpt-4.1-mini`
2. **`lib/openai.ts`** or new `lib/elevenlabs.ts` — Replace TTS with ElevenLabs API
3. **`components/DetailView.tsx`** — Remove Simply, word reasoning, Grammar & Phrases, word explanations
4. **`components/ColorCodedSentence.tsx`** — Remove hover/click interactivity, keep colors
5. **`lib/types.ts`** — Clean up types if fields are removed from prompt
6. **`app/api/analyze/route.ts`** — Update prompt to stop generating removed fields
7. **`app/api/audio/route.ts`** — Update audio generation to use ElevenLabs
