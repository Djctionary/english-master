# v0.3 — Review Page Overhaul

**Date:** 31/03/2026

## Requirements

### Core Review Flow
1. Auto-play audio after clicking "Next sentence"
2. Show sentence meaning (paraphrase) on reveal — not "listening anchors"
3. After choosing a result, show compact breakdown: paraphrase, vocabulary (word + phonetic + definition), sentence skeleton
4. Limit audio replay to 3 times per card (show remaining count)

### Stats & Session
5. Replace four stat blocks with: Total sentences, Due for review, Reviewed (session), Mastered (stage 8)
6. Session complete: show encouragement message + session stats after finishing 10 cards

### Cleanup
7. Remove `buildListeningHighlights()` from `lib/review.ts` — replaced by compact breakdown
8. Gracefully handle old data with empty/verbose fields

### UI/UX
9. Modern, minimal design — cleaner cards, better spacing, refined typography
10. Consistent visual language with learn page

## Implementation Plan

1. `app/api/review/route.ts` — return new stats (total, due, mastered, session count)
2. `lib/review.ts` — remove `buildListeningHighlights()`
3. `lib/types.ts` — update `ReviewQueueResult` type for new stats
4. `app/review/page.tsx` — full rewrite: new stats, compact breakdown, auto-play, replay limit, session complete, modern UI
5. `lib/sentence-store.ts` — add queries for total/mastered counts
6. `docs/specs/BACKLOG.md` — remove shipped items
7. `docs/specs/CHANGELOG.md` — append v0.3
